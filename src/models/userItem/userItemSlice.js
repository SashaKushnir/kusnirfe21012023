import {createSlice} from "@reduxjs/toolkit";

export const usersListSlice = createSlice({
    name: 'users-list',
    reducers: {
        setDefaultUsersList: (state: RootState) => {

        }
    }
})